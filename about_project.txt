Chunked Transfer Encoding: not used in http/1.0
to check the timeout -> curl http://127.0.0.1:8080/
Serving HTTP on 127.0.0.1 port 8090 (http://127.0.0.1:8090/) ... [*]
Keyboard interrupt received, exiting.
413 Payload Too Large.
Edge-triggered mode & level-triggered mode (ET & LT)
New connection handeling on "accept()"
defult extension (.html -> text/html)

#1 -> same host in diff server-block, (another case "127.0.0.1 == localhost")
#2 -> remove the server_name
#3 -> know about MIME map


time_out
client_max_body_size 4000000;

-------------------------------------------------

content-length: 10byte;
body {} = 1GB

read just 10 and set the request is ready.
my job now wil be checking the 

-------------------------------------------------

content-length: 1GB
body {} = 10byte

-------------------------------------------------

            // if (x != OK)
            {
                std::cout << "THE FUCKING REQUEST HAS CATCHING SOMETHING NOT VALID [ " << x << " ]" << std::endl;
                std::cout << "--------------------------------------------" << std::endl;
                std::cout << "Method :" << this->raw_client_data[fd].req.getMethod() << std::endl;
                std::cout << "Path :" << this->raw_client_data[fd].req.getPath() << std::endl;
                std::cout << "Query :" << this->raw_client_data[fd].req.getQuery() << std::endl;
                std::cout << "HTTP version :" << this->raw_client_data[fd].req.getHttpVersion() << std::endl;
                std::cout << "--------------------------------------------" << std::endl;
                std::map<std::string, std::string>::iterator it;
                std::map<std::string, std::string> headert = raw_client_data[fd].req.getHeaders();
                for (it = headert.begin(); it != headert.end();it++)
                {
                    std::cout << it->first << " :" << it->second << std::endl;
                }
            }
-------------------------------------------------

// TODO NOW:
- 400 bad request in case of request start with ./ [to chech]
- check the path edge cased all of them [*]
- normalize the path before configparsing [to check]
- set the time_out checker  [working on it]
- check out what will happen when the client reatch time_out [*]
- check out the return behaver  []
- header_gen(); []
- body_gen(); []
- response_buffer[]

// OTHERS
- content-length && body-client_max_body_size ()
/*  TO-KNONW ABOUT THE RESPONCE HEADER INFO
        start-line gen [*]
        server-name [*]
        date gen [*]
        content-length [*] based on the body will send
        content-type [*]
        cache-control [] -> i don't know what is for, just ignore it?
*/

TOKNOW:
        recv flag for?
        how server handle request?
        diff blocking/non-blocking?
        single threaded webserv?

THIS IS ALL WHAT KNOW ABOUT THE CODE THAT I HVAE MADE:
#1: first of all i based on the config file parsing by taking the infos (like; host and listening port) and every single server block i create a server socket for it to server the content of the server block.
here i have to know what is the sockets, right? yeh
the sockets is a pre-build in function in linux kernal that help use to make a comunication between two precess
(i have another confusing about is comunication between two interfaces or process????, first we have a process then use the interface to make the process comunication with the other process, right?)
over the network or localy using NETWORK layers, exactly in transport layer
socket is the way to route the msg to destination based on the host and port?
example socket is just a softwear(phone) and need a address(host, port) to done it's job (like a phone number)
"i have another question in my minde if the transponrt layer is where socket is operate, in case of havin a request mean's will start from top Application layer to the pottom that mean's the socket will not get the host becouse the traspoort layer that link between two devices had access to just ports, the network layer the has the ips is that mean's the socket init start and stop just in the transport layer???"
start with how socket life cicel is
first of all create a socket like this:

socket(result->ai_family, result->ai_socktype, result->ai_protocol)
by the help of the getaddrinfo i have the host and ip is a string format that the getaddrinfo do the DNS resulotion for me and giving back
ai_family: what type of ip version that socket will be v4 or v6
ai_soktype: type of the socker udp or tcp
lats think what is the job of that socket
ai_flags for me i remove it becouse i don't use it
last one i set it with 0 to get the defult (tcp/ip) for http protocol
and the socket is just a softwear structer the take to kearel space in case of send and recv but at first it gives you back an file desciptor back to use it later on
by seting the socket seccussfully i will create a epoll_event struct for every new socket is created
as i say socket return fd for the socket that will be created the assinge the epoll_event struct with the create socket info like the return socket fd:
new_server_ev.data.fd = serv_socketFD; // serv_socketFD is the return of the socket
new_server_ev.events = EPOLLIN; // EPOLLIN is the behavior needed that socket to done, EPOLLIN -> tell me when data availabel to read, but when i tell you that socket return an fd that mean's we can use the read() function that like the recv without the flags

after we get the socket fd and init the struct epoll_event call the setsocketopt()
-> setsockopt(serv_socketFD, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt)) < 0)
this line fix a problem when using a ip address and bild it to a socket, killing the program at the end, without this like the kernel set a TIME_WAIT counter, mean's you can not use the ip address agine intel the time it up.
if we try will bind will have error of 'bind' failed: Address already in use
SOL_SOCKET -> make what level of the socket to apply the change to it
SO_REUSEADDR -> the actual change is make the address free without any TIME_WAIT

next i call the fcntl before i bind the socket with an address will talk about that later on
fcntl is a file control that take a fd as parame to know witch file to control, as the file open i can control it wit fcntl
here to use the fcntl i have to know what is the deffrence and behaver of blocking and non-blocking
both of them handeling the input/output just in a deffrete way
start with the defult behaver of sockete:
blocking socket is effect the four functions: accepte() recv(), send(), connect()
before get in the blocking and non-blocking needs to know what is accepte() recv(), send(), connect();
accepte() -> is that server step, of socket initation, accept() called when there's a new client connection
before accepte there's a methode called
- listen all it's done change the status of the socket to listen mode.

to perform handshakes on server port, just in case the client ping to the server port that listen() listen from, the client
send to the listen server port a SYN(packet) then the server with help of listen done the handshak, right?
NOTE: in case of client send a SYN(packet) in case of the handshak the kernal record the info like the (IP, PORT) from the SYN(packet)
the we call the accept() syscall to get a new socket with client info alrady on it
who captuer the SYN packet form the client when i ping to the server port????
listen params(server-socket-fd, backlog)
first param -> just a server-side socket
second param -> when server listen on a new event and record it's info it host that client info to a queue, intel call accept() to handel the client request
that the job of the second param but is just a number of the max client will hold in the queue 


okay there's tow part of the request parsing:
    a valid request syntax, but the request is not allowd and
    that will be handeld by the response error
but the second case i have have a problem with the syntax it's self
    like the fucking methode that has be giving is like "GGGET"
    and that will be not valid but i overthink about some thing else
    what if the methode like GEEET is not allowed i can stell me and
    exaption about it and send a resp to the client and tell them method not allowd
    405, based on the host, and that the fucking trick what if the host is not valid too

epoll complixety is O log 1, but poll is O log n(based on client number)


// WOKING ON IT
/*  TODO:
    get the Host from the request header (in case of; GET, POST or DELETE)???
    extract the host && parsing it to [host, port] then check using
        server_conf = getServerForRequest(s_host, _port_, config);
        location_conf = getLocation(this->path, *server_conf); 
    then check if the Host info is there in server block if yes serve it
    otherways error page Bad request 400
    at the end of a problem will call 
    this->raw_client_data[fd].res.set_stat_code(BAD_REQUEST);

    // --------------------------------------------------------------------------
    int s_host = address_resolution(_host_);
    server_conf = getServerForRequest(s_host, _port_, config);
    if (server_conf == NULL)    // ???
        current_client.res.set_stat_code(SERVER_ERROR);
    
    // --------------------------------------------------------------------------
    this->path = path_resolver(PATH0);
    location_conf = getLocation(this->path, *server_conf);
    if (location_conf == NULL) {
        std::cout << "THE FUCKING LOCATION PATH IS NOT EXIST IN THE SERVER BLOCK" << std::endl;
        exit(0);
        current_client.res.set_stat_code(NOT_FOUND);
    }
    // --------------------------------------------------------------------------

*/

/*  TODO-FIX
    U have to read just the header at first what ever there's a body or not
    for me a will take the header and get you witch server block matched or not
    if there's a match i'll give you back
        client_max_body_size
        if the method is allowd
        the cgi
*/// --------------------------------------------------------------------------

/*  TODO-FIX 'what i have to done' as @ma1loc
    take provided header and looking for Host
        when i get it will splite it: addr, port
        then will take the addr, port: search on my config file to get a match
        if there's a match will give you back
            client_max_body_size
            allowd method
            cgi
*/
// TODO: a methode to take the raw req and extract from it info
// -------------------------------------------------------------------------------                

// ----------------- HARDCODED ------------------
// HEADER Host -> server { ... } block to use
// _host_, _port_, _methode -. getting it form the request


// --------------------------------------------------------------------------
// PATH REQUEST TEST
// # define PATH0  "/www/index.html"                                        // PASS +
// # define PATH0  "/www/../"                                               // PASS +
// # define PATH0  "/www/../www"                                            // PASS +
// # define PATH0  "/www/../www/index.html"                                 // PASS +
// # define PATH0  "/www/../www/secret.html"                                // PASS +
// # define PATH0  "/www/../../../../../yanflous/Documents/index.html"      // PASS +
// ------------------------------------------------------
// request normalization test
// # define PATH0  "/./www/index.html"                                      // PASS +
// # define PATH0  "/./www//index.html"                                     // PASS +
// # define PATH0  "//www//index.html"                                      // PASS +
// # define PATH0  "./www/index.html"                                       // PASS +
// ------------------------------------------------------
// # define PATH0  "/www"                                                   // PASS +
// # define PATH0  "/www/"                                                  // PASS +
// # define PATH0   "www/secret.html"                                       // PASS +
// # define PATH0   "www/my%20file.html"                                    // PASS +
// ------------------------------------------------------

CGI:
QUERY_STRING if provided in the URL
    behaver: 